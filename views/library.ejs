<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title></title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <h1>LIBRARY</h1>
    <a href="/users/profile">HOME</a>
    <div id="favorites-container">
      <h2><%= user.username %>'s Favorites:</h2>
      <% favorites.forEach((memeFave) => { %>
      <div class="faveMeme">
        <p><%= memeFave.urlName %></p>
        <img src="<%= memeFave.imageUrl %>">
        <form action="/library/<%= memeFave._id %>?_method=DELETE" method="post">
          <input type="submit" value="Delete">
        </form>
      </div>
      <% }) %>
      <% favorites.forEach((gifFave) => { %>
      <h1>fav gif</h1>
      <div class="faveGif">
        <p><%= gifFave.slug %></p>

        <form action="/library/<%= gifFave._id %>?_method=DELETE" method="post">
          <input type="submit" value="Delete">
        </form>
      </div>
      <% }) %>
  </div>
  </body>
</html>
